package ngs.view{  import flash.display.MovieClip;  import com.greensock.TweenLite;  import com.greensock.easing.*;  import ngs.model.Session;  import flash.events.MouseEvent;  public class PageTwo extends MovieClip  {    public var _session:Session;        public function PageTwo()    {          }        public function set session(m:*):void    {      _session = m;      register();      init();    }        public function get session():Session    {      return _session;    }        public function register():void    {      title.def_y = title.y;      subtitle.def_y = subtitle.y;    }        private function configure():void    {          }        public function init():void    {          }        public function transitionIn():void    {         treemap.visible = false;      this.alpha = 1;      TweenLite.to(this, 0.5, { onComplete: transitionInComplete } );            title.y = title.y + title.height;      subtitle.y = subtitle.y + subtitle.height;            TweenLite.to(title, 0.4, { y: title.def_y, ease: Back.easeOut } );      TweenLite.to(subtitle, 0.4, { y: subtitle.def_y, ease: Back.easeOut, delay: 0.25, onComplete: treemap.transitionIn } );    }        public function transitionOut():void     {      session.lockNavigation();      TweenLite.to(this, 0.5, { alpha: 0, onComplete: transitionOutComplete } );    }        public function transitionInComplete():void    {      session.unlockNavigation();    }        public function transitionOutComplete():void    {      session.updateView();      parent.removeChild(this);    }  }}